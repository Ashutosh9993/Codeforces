#include<iostream>
using namespace std;
int easyarr(int arr[],int n){for(int i=0;i<n;i++){if(arr[i]!=0)return 0;}return 1;}
int main(){
    int n;cin>>n;int arr[n];
    for(int i=0;i<n;i++)cin>>arr[i];
    if(easyarr(arr,n)){cout<<"EASY";else}{cout<<"HARD";}
    return 0;
}
