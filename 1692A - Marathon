#include<bits/stdc++.h>
using namespace std;

int main(){
int n;cin>>n;
while(n--){
    int x,a,b,c;cin>>x>>a>>b>>c;int ans=0;
        if(a>x)ans++;
        if(b>x)ans++;
        if(c>x)ans++;
    cout<<ans<<endl;
}
    return 0;
}
